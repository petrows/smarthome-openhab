rule "M800s read status"
when
    Item astro_sun_el received update
then
    if (astro_sun_el.state < 5.0 | "°") {
        astro_is_night.sendCommand(ON)
    } else {
        astro_is_night.sendCommand(OFF)
    }
end
